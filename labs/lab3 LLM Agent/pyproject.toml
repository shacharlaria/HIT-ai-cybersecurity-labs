[project]
name = "ag2-chainlit-demo"
version = "0.1.0"
description = "Demo: AG2 agents + Chainlit UI with Groq (OpenAI-compatible API)"
readme = "README.md"
requires-python = ">=3.11"

dependencies = [
    "ag2[openai]>=0.9",
    "chainlit>=2.0",
]
